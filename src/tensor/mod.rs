//! Tensor module: defines the Tensor struct and basic tensor utilities.

pub struct Tensor {
    // shape: Vec<usize>,
    // data: Vec<f32>, etc.
}

impl Tensor {
    pub fn new() -> Self {
        todo!("Tensor implementation not written yet")
    }

    pub fn shape(&self) -> &[usize] {
        todo!("Tensor::shape implementation not written yet")
    }
}

